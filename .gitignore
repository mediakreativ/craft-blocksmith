# Node modules
node_modules/

# Composer vendor folder
vendor/

# Composer lock file – not committed for plugins/libraries.
# The lockfile only affects this plugin's own dev environment; consumers
# resolve dependencies against their own project's composer.lock.
composer.lock

# macOS system files
.DS_Store

# Log and debug files
*.log
*.log.*

# Build artefacts (falls später ein Build-Prozess verwendet wird)
dist/
build/

# Craft runtime/temp files
storage/
.env
.env.*
web/cpresources/

# Ignoriere Translations Cache, falls es existiert
translations/cache/

# VSCode-specific files
.vscode/
